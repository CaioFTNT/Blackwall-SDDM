import QtQuick
import QtQuick.Controls

Pane {
    id: root

    width: Screen.width; height: Screen.height

    background: Image {
        anchors.fill: parent
        source: "background.png"
    }
}